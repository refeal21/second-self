{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-workbench-events",
  "description": "Allow the main window to receive typed stdout and exit events from Rust-owned local processes.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
